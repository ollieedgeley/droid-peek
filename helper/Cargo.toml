[package]
name = "droid-peek-helper"
version = "1.0.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
qrcode = { version = "0.14", default-features = false, features = ["svg"] }
nix = { version = "0.29", default-features = false, features = ["fs", "process", "signal"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tempfile = "3"
zeroize = "1"

[lib]
name = "droid_peek_helper"
path = "src/lib.rs"

[[bin]]
name = "droid-peek-helper"
path = "src/main.rs"
